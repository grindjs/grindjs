export function up(db) {
	return db.schema.createTable('StubTable', table => {
		table.increments('id').unsigned().primary()
		table.timestamps()
	})
}

export function down(db) {
	return db.schema.dropTable('StubTable')
}
